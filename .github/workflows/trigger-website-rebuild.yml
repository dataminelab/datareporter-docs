name: Trigger Website Rebuild

on:
  push:
    branches: [main]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger website deploy
        run: |
          gh workflow run deploy.yml --repo dataminelab/website --ref main
        env:
          GH_TOKEN: ${{ secrets.WEBSITE_DEPLOY_TOKEN }}
